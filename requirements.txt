# Core app
streamlit>=1.39,<2.0
plotly>=5.18,<6.0

# Data / ML
pandas>=2.2,<2.3
numpy>=2.0,<2.3
scikit-learn>=1.5,<1.6

# Deep learning
torch>=2.3,<2.6
pytorch-lightning>=2.3,<2.6

# darts: the 'darts' PyPI package (same lib as u8darts, just current naming).
# Modern darts has a slim default install; heavy backends only via extras.
darts>=0.44,<0.45
