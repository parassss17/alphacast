# Core app
streamlit>=1.39,<2.0
plotly>=5.18,<6.0

# Data / ML
pandas>=2.2,<2.3
numpy>=2.0,<2.3
scikit-learn>=1.5,<1.6

# Deep learning
torch>=2.3,<2.6
pytorch-lightning>=2.3,<2.6

# darts: 'u8darts[torch]' installs ONLY the torch-model subset
# (skips heavy optional deps like prophet, lightgbm, xgboost, statsmodels
# that often fail to build on Streamlit Cloud).
u8darts[torch]>=0.44,<0.45
